import 'package:flutter/material.dart';

class UserProductSc extends StatelessWidget {
  const UserProductSc({Key key}) : super(key: key);

  static const routeName = '/user-product';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
